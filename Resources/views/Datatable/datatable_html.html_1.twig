<table cellpadding="0" cellspacing="0" class="{{ view_style }}" border="0" id="{{ view_table_id }}" width="100%">
    <thead>
    </thead>
    {% if view_individual_filtering %}
        <thead>
            {% for column in view_columns %}
                <th>
                    {% if column.searchable %}
                        <input type="text" class="individual_filtering" placeholder="{{column.title|striptags}}" />
                    {% else %}
                        <input type="hidden" />
                    {% endif %}
                </th>
            {% endfor %}
        </thead>
    {% endif %}
        <tfoot>
        </tfoot>
    <tbody>
    </tbody>
</table>
